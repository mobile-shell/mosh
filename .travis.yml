os:
  - linux
  - osx

cache:
  - apt

language: cpp
sudo: required
services: docker

addons:
  apt:
    packages:
    - protobuf-compiler
    - libprotobuf-dev
    - libutempter-dev
    - tmux			# test suite
    - perl			# test suite

before_install:
  - test "$TRAVIS_OS_NAME" = osx && brew update
  - test "$TRAVIS_OS_NAME" = osx && brew outdated protobuf tmux || brew upgrade protobuf tmux
  - git fetch --tags --unshallow

script:
  - ./autogen.sh
  - ./configure --enable-compile-warnings=error --enable-examples
  - make distcheck

notifications:
  irc: "chat.freenode.net#mosh"
